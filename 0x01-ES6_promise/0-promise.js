export default function getResponseFromAPI() {
  return new Promise(() => {
    // return new Promise(function(resolve, reject) {} works too
    // fetch something e.g data and make a request
    // if it is a success call the resolve function and pass data
    // if there is an error call reject and pass the error 
  });
}
